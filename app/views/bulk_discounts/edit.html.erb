<%= form_with url: merchant_bulk_discount_path(@bulk_discount_edit.merchant_id, @bulk_discount_edit.id), method: :patch, local: true do |f|  %>
  <%= f.label :percentage_discount %>
  <%= f.text_field :percentage_discount, value: @bulk_discount_edit.percentage_discount%>

  <%= f.label :quantity_threshold %>
  <%= f.text_field :quantity_threshold, value: @bulk_discount_edit.quantity_threshold %>

  <%= f.submit "Submit" %>
<% end %>
